const { expect, assert } = require( 'chai' );

describe( 'Users', function()
{
    it( 'Перегляд списку користувачів', function()
    {
        assert.equal( [ 1, 2, 3 ].indexOf( 4 ), -1 );
    } );
} );
